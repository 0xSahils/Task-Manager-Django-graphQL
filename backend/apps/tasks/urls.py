from django.urls import path
from rest_framework.routers import DefaultRouter

app_name = 'tasks'

# REST API URLs will be added here when we create ViewSets
urlpatterns = [
    # URL patterns will be added when we implement REST views
]
